{{- if .Values.frontend.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "simpletickr.frontend.fullname" . }}
  labels:
    {{- include "simpletickr.frontend.labels" . | nindent 4 }}
spec:
  {{- if .Values.frontend.ingress.className }}
  ingressClassName: {{ .Values.frontend.ingress.className }}
  {{- end }}
  rules:
    - host: {{ .Values.frontend.ingress.hostname }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "simpletickr.frontend.fullname" . }}
                port:
                  name: http
  {{- if .Values.frontend.ingress.tls }}
  tls:
    - hosts:
        - {{ .Values.frontend.ingress.hostname }}
      secretName: {{ include "simpletickr.frontend.fullname" . }}-tls
  {{- end }}
{{- end }}
