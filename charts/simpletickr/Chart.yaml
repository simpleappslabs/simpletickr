apiVersion: v2
name: simpletickr
description: Simple portfolio tracker — backend and frontend
type: application
version: 0.1.0
appVersion: "latest"
dependencies:
  - name: postgresql
    version: "16.x.x"
    repository: https://charts.bitnami.com/bitnami
    condition: postgresql.enabled
