DB_NAME=simpletickr
DB_USER=simpletickr
DB_PASSWORD=simpletickr

# URL the browser uses to reach the API.
# Change this to your server's hostname or IP if accessing from another machine.
PUBLIC_API_BASE_URL=http://localhost:8080